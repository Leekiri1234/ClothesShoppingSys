# Home UI

This folder organizes templates for the storefront landing experience as modeled by the legacy `page-home` component.

## Expected Templates:
- `index.html` (or mapped to `home.html` in the root of the templates folder): Contains the hero image, featured collections, best sellers, flash sale, and newsletter sections.
