# Try-On UI

This folder serves the templates handling the virtual fitting room feature documented in the legacy `page-try-on`.

## Expected Templates:
- `index.html`: The main try-on view with camera input, file upload, and rendering the simulated try-on output.
