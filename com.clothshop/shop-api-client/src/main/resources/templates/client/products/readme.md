# Products UI

This folder contains the core product browsing and details templates, equivalent to `page-products` and `page-product-detail` in the legacy layout.

## Expected Templates:
- `index.html` or `list.html`: The main shop catalog with filters (category, color, price) and sorting.
- `detail.html`: Individual product view with images, variants, an "add to cart" form, and reviews.
