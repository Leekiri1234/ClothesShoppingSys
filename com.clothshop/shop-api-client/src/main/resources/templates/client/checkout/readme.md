# Checkout UI

This folder should contain templates related to the checkout process, as seen in the legacy `page-checkout` section.

## Expected Templates:
- `index.html` or `checkout.html`: The checkout form collecting shipping addresses and payment methods.
