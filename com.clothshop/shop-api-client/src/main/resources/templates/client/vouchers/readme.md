# Vouchers UI

This folder contains templates for user vouchers/coupons, typically linked from the profile dashboard.

## Expected Templates:
- `index.html` or `list.html`: Shows a list of available and used discount codes to the logged-in user.
